schema {
    query: Query
    mutation: Mutation
}

type Query {
    GET_POST(id: ID!): Post!
}

type Mutation {
    CREATE_POST(post: PostInput): Post
}

input PostInput {
    name: String
    description: String 
}
type Post {
    id: ID
    name: String
    description: String 
}
